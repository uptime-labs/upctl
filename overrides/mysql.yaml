auth:
  rootPassword: uptimelabs
  createDatabase: true
  database: uptimelabs
  username: uptimelabs
  password: uptimelabs

primary:
  service:
    type: LoadBalancer

initdbScripts:
  createdb.sql: |-
    CREATE DATABASE IF NOT EXISTS keycloak;
    GRANT ALL PRIVILEGES ON *.* TO 'uptimelabs'@'%';
    FLUSH PRIVILEGES;
